<%= radio_button_tag 'flight_id', flight.id %>
<%= "#{flight.departure_airport.code} ---> #{flight.destination_airport.code}" %>   
<%= "Departs: #{flight.date.to_formatted_s} - Duration: #{time_ago_in_words(flight.duration.minutes.from_now)}" %><br>